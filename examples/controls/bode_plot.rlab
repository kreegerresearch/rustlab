# bode() — Bode magnitude and phase frequency response

s = tf("s")
G = 10 / (s^2 + 2*s + 10)

# Return magnitude (dB), phase (deg), and frequency (rad/s) vectors
[mag, phase, w] = bode(G)
semilogx(w, mag, "Bode Magnitude |G(jw)| (dB)")
semilogx(w, phase, "Bode Phase /G(jw) (deg)")
